[project]
name = "livekit-voice-agent"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
    "livekit-agents[cartesia, openai]",
    "python-dotenv",
    "ddgs>=9.14.4",
    "livekit>=1.1.12",
    "requests>=2.34.2"
]
